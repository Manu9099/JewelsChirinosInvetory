namespace JewelShrinos.Core.Enums
{
    public enum SunatDocumentType
    {
        Invoice = 1,        // Factura (01)
        Receipt = 2,        // Boleta (03)
        CreditNote = 3      // Nota de Crédito (07)
    }
}