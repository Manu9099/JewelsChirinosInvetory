namespace JewelShrinos.Core.Enums
{
    public enum PurchaseStatus
    {
        Pending = 1,        // Pendiente
        Confirmed = 2,      // Confirmada
        Received = 3,       // Recibida completamente
        Partial = 4,        // Recibida parcialmente
        Cancelled = 5       // Cancelada
    }
}